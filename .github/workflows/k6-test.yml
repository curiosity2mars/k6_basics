name: k6 Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: k6 Load Test
      uses: grafana/k6-action@v0.2.0
        with:
          filename: test.js
